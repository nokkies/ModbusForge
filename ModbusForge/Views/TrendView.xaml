<UserControl x:Class="ModbusForge.Views.TrendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:converters="clr-namespace:ModbusForge.Converters"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock Text="Series:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox Width="220"
                      ItemsSource="{Binding SeriesItems}"
                      SelectedItem="{Binding SelectedSeriesItem}"
                      DisplayMemberPath="Name"
                      />
            <Button Content="Delete" Margin="6,0,0,0" Width="80" Command="{Binding DeleteSelectedCommand}"/>
            <Button Content="Color" Margin="6,0,0,0" Width="80" Command="{Binding ChangeColorCommand}"/>
            <Separator Width="10" Opacity="0"/>
            <Button Content="Reset" Width="80" Command="{Binding ResetViewCommand}"/>
            <Button Content="Play" Width="80" Margin="6,0,0,0" Command="{Binding PlayCommand}"/>
            <Button Content="Pause" Width="80" Margin="6,0,0,0" Command="{Binding PauseCommand}"/>
            <Separator Width="10" Opacity="0"/>
            <CheckBox Content="Lock X" Margin="6,0,0,0" VerticalAlignment="Center" IsChecked="{Binding LockX}"/>
            <CheckBox Content="Lock Y" Margin="6,0,0,0" VerticalAlignment="Center" IsChecked="{Binding LockY}"/>

            <!-- Retention minutes control -->
            <Separator Width="20" Opacity="0"/>
            <TextBlock Text="Retention (min):" VerticalAlignment="Center"/>
            <TextBox Width="60" Margin="6,0,0,0" VerticalAlignment="Center"
                     Text="{Binding RetentionMinutes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Apply" Width="70" Margin="6,0,0,0" Command="{Binding ApplyRetentionCommand}"/>

            <!-- Export/Import -->
            <Separator Width="20" Opacity="0"/>
            <Button Content="Export CSV" Width="100" Margin="0,0,0,0" Click="ExportCsv_Click"/>
            <Button Content="Import CSV" Width="100" Margin="6,0,0,0" Click="ImportCsv_Click"/>
            <Button Content="Export PNG" Width="100" Margin="6,0,0,0" Click="ExportPng_Click"/>
        </StackPanel>

        <lvc:CartesianChart x:Name="TrendChart" Grid.Row="1"
                             Series="{Binding Series}"
                             XAxes="{Binding XAxes}"
                             YAxes="{Binding YAxes}"
                             LegendPosition="Right"
                             TooltipFindingStrategy="CompareOnlyX">
            <lvc:CartesianChart.ZoomMode>
                <MultiBinding Converter="{StaticResource LockToZoomModeConverter}">
                    <Binding Path="LockX"/>
                    <Binding Path="LockY"/>
                </MultiBinding>
            </lvc:CartesianChart.ZoomMode>
        </lvc:CartesianChart>
    </Grid>
</UserControl>
